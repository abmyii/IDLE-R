Add all menuBar options

When import, add all namespaces of the module for auto-completion
(If import in line, then eval loc and if worked, add namespaces)

When there are braces, show doc str of the module and arg called with showToolTip

Bracket matching

Use (widget).addAction for auto-completion. For __init__ of the widget, set parent to QTextEdit

Make my own console (shell) using QtGui.QDockWidget, and add it to main window 
using self.addDockWidget. (? Use ipython console?)

Make colorscheme customizable (and editor bg?) ?

# Key bindings
F5 = Python2 Run
F6 = Python3 Run


Project browsing
